---

- name: Install Asterisk requirements
  apt: 
    name: "{{ packages }}"
    state: present
  vars:
    packages:
    - asterisk
  register: my_result
  until: my_result is succeeded
  tags: asterisk
